name: Build APK - Gradle Wrapper Fixed

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Extract Android Project
      run: |
        echo "📦 Extracting project..."
        unzip -q android_webview_project.zip
        echo "✅ Extracted successfully"
        ls -la android_webview/
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      with:
        api-level: '34'
        build-tools-version: '34.0.0'
    
    - name: Check and Fix Gradle wrapper
      working-directory: ./android_webview
      run: |
        echo "🔍 Checking Gradle wrapper files..."
        
        # Check what wrapper files exist
        ls -la gradlew* 2>/dev/null || echo "No gradlew files found initially"
        
        # Check if gradle wrapper directory exists
        if [ -d "gradle/wrapper" ]; then
          echo "📁 Found gradle/wrapper directory"
          ls -la gradle/wrapper/
        else
          echo "⚠️  No gradle/wrapper directory found"
        fi
        
        # Set permissions only for existing files
        echo "🔧 Setting permissions for existing Gradle wrapper..."
        if [ -f "gradlew" ]; then
          chmod +x gradlew
          echo "✅ Set permissions on gradlew"
        fi
        
        if [ -f "gradlew.bat" ]; then
          chmod +x gradlew.bat  
          echo "✅ Set permissions on gradlew.bat"
        fi
        
        # If no wrapper exists, we need to create one
        if [ ! -f "gradlew" ] && [ ! -f "gradlew.bat" ]; then
          echo "🔧 No Gradle wrapper found, will generate one..."
        fi
        
        echo "📋 Current wrapper status:"
        ls -la gradlew* 2>/dev/null || echo "Still no wrapper files"
    
    - name: Generate Gradle wrapper if missing
      working-directory: ./android_webview
      run: |
        echo "🔧 Checking if we need to generate wrapper..."
        
        if [ ! -f "gradlew" ]; then
          echo "⚠️  gradlew not found, generating wrapper..."
          # Use gradle to generate wrapper
          gradle wrapper --gradle-version 8.0
          chmod +x gradlew
          echo "✅ Generated gradlew wrapper"
        else
          echo "✅ gradlew already exists"
        fi
        
        # Verify wrapper exists
        if [ -f "gradlew" ]; then
          echo "✅ gradlew wrapper is ready"
          ./gradlew --version
        else
          echo "❌ ERROR: Cannot create gradlew wrapper"
          exit 1
        fi
    
    - name: Set Gradle environment variables
      working-directory: ./android_webview
      run: |
        echo "🔧 Setting Gradle environment variables..."
        
        # Set environment variables
        export GRADLE_OPTS="-Xmx2g -Dfile.encoding=UTF-8"
        export JAVA_OPTS="-Xmx2g -Dfile.encoding=UTF-8"
        
        # Export to GitHub environment
        echo "GRADLE_OPTS=$GRADLE_OPTS" >> $GITHUB_ENV
        echo "JAVA_OPTS=$JAVA_OPTS" >> $GITHUB_ENV
        
        echo "✅ Environment variables set"
        echo "GRADLE_OPTS: $GRADLE_OPTS"
        echo "JAVA_OPTS: $JAVA_OPTS"
    
    - name: Clean project thoroughly
      working-directory: ./android_webview
      run: |
        echo "🧹 Cleaning project directories..."
        
        # Remove all build artifacts
        if [ -d "app/build" ]; then
          rm -rf app/build/
          echo "✅ Removed app/build/"
        fi
        
        if [ -d "build" ]; then
          rm -rf build/
          echo "✅ Removed build/"
        fi
        
        if [ -d ".gradle" ]; then
          rm -rf .gradle/
          echo "✅ Removed .gradle/"
        fi
        
        echo "🔄 Testing Gradle wrapper..."
        ./gradlew --version --no-daemon
    
    - name: Build APK
      working-directory: ./android_webview
      run: |
        echo "🏗️ Starting APK build..."
        echo "📊 Build configuration:"
        echo "  - Environment: Linux/Ubuntu"
        echo "  - Java: JDK 17"
        echo "  - Gradle: Wrapper (./gradlew)"
        echo "  - Build type: Debug"
        
        # Build APK with all environment variables
        ./gradlew assembleDebug \
          --no-daemon \
          --stacktrace \
          --continue
        
        BUILD_EXIT_CODE=$?
        
        if [ $BUILD_EXIT_CODE -eq 0 ]; then
          echo "✅ APK build completed successfully!"
        else
          echo "❌ APK build failed with exit code: $BUILD_EXIT_CODE"
          echo "🔍 Checking for partial build artifacts..."
          find app/build -name "*.apk" -type f 2>/dev/null || echo "No APK files found"
          exit $BUILD_EXIT_CODE
        fi
    
    - name: Verify APK exists
      run: |
        APK_PATH="android_webview/app/build/outputs/apk/debug/app-debug.apk"
        echo "🔍 Verifying APK creation..."
        
        if [ -f "$APK_PATH" ]; then
          echo "✅ SUCCESS: APK created successfully!"
          echo "📱 APK Details:"
          ls -lh "$APK_PATH"
          file "$APK_PATH"
          echo "📏 APK Size: $(du -h "$APK_PATH" | cut -f1)"
        else
          echo "❌ ERROR: APK not found at expected location"
          echo "🔍 Searching for any APK files..."
          find android_webview -name "*.apk" -type f
          echo "🔍 Checking build directory structure:"
          find android_webview/app/build -type f | head -10
          exit 1
        fi
    
    - name: Upload APK (v4)
      uses: actions/upload-artifact@v4
      with:
        name: webview-apk-debug
        path: android_webview/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
    
    - name: Upload project source
      uses: actions/upload-artifact@v4
      with:
        name: project-source-complete
        path: android_webview/
        retention-days: 30
    
    - name: Upload build logs
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-debug
        path: |
          android_webview/app/build/
          android_webview/.gradle/
        retention-days: 7
